struct
{
	int pipe[2]; y
	char **cmd/red; -> {"|", NULL} {<, file/delimiter} {cmd, parameter}
	struct *next;
	struct *prev;
}


< outfile grep a | cat >> outfile
strcut1 -> cmd = { <, outfile, NULL}
struct2 -> cmd = { grep, a, NULL}
struct3 -> cmd = { |, NULL}
struct4 -> cmd = { cat, NULL}
struct5 -> cmd = { >>, outfile, NULL}
